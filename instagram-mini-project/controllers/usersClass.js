const db = require('../db');

class usersInsta{
   constructor ({id,name,username, email,avatar_url}){
   this.id = id;
   this.name = name;
   this.username = username;
   this.email = email;
   this.avatar_url = avatar_url;
   }
//static async
//return
}

module.exports = usersInsta;